micromegas uses the .mdl files in h4GOn (or h4gOff)
so copy them to the work/model directory of the generated model directory in micromegas.
The .mdl files were generated using TRSM_mixed.mdl by lanhep,
which is saved in lanhep/ for documentation.